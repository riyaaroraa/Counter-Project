RollOverCounter.java = A counter that resets every-time the maximum count is reached.

LimitedCounter.java = A counter that stops counting every-time the maximum count is reached.

Counter.java = A generic counter object. A super class for RollOverCounter